<!DOCTYPE html>
<html lang="en">
<%- include('partial/head') %>

<body>
  <%- include('partial/navbar') %>
  <div class="container-fluid">
    <h1 class="text-center">User List</h1>
    <a href="/add">
      <button class="btn btn-primary mb-2 mt-2">Add List</button>
    </a>
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>ID</th>
          <th>Email</th>
          <th>Password</th>
          <th>Name</th>
          <th>Hobby</th>
          <th>Age</th>
          <th>Win</th>
          <th>Lose</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% data.forEach(item => { %>
        <tr>
          <td><%= item.uuid %> </td>
          <td><%= item.email %> </td>
          <td><%= item.password %> </td>
          <td><%= item.user_game_biodata.name %> </td>
          <td><%= item.user_game_biodata.hobby %> </td>
          <td><%= item.user_game_biodata.age %> </td>
          <td><%= item.user_game_history.win %> </td>
          <td><%= item.user_game_history.lose %> </td>
          <td>
            <div class="row">
              <div class="col-6">
                <a href="/edit?id=<%= item.uuid %>">
                  <button class="btn btn-warning">
                    Edit
                  </button>
                </a>
              </div>

              <div class="col-6">
                <form action ="/delete?id=<%= item.uuid %>" method="POST">
                  <button class="btn btn-danger">
                    Delete
                  </button>
                </form>
              </div>
            </div>


          </td>
        </tr>
        <% }) %>
      </tbody>
      <script src="/js/bootstrap.min.js"></script>
</body>

</html>